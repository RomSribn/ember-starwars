<div class="container h-100 d-flex">
    <div class="row m-auto position-relative">
        {{#each this.model.fighters as |f|}}
        <Card @person={{f}} />
        {{/each}}
        <ButtonHome />
    </div>
</div>

